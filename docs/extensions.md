# Extension System

Detour supports Chrome-compatible browser extensions using the Manifest V3 format. The implementation covers content scripts, background service workers, popup UI, and a subset of `chrome.*` APIs — enough to run extensions that inject scripts, store data, query tabs, and communicate between content and background contexts. Extensions are loaded from unpacked directories and managed through `ExtensionManager`, which coordinates injection, background hosting, messaging, and toolbar integration.

## Architecture

```
ExtensionManager.shared (singleton)
  ├── extensions: [WebExtension]         loaded extension models
  ├── injector: ContentScriptInjector    injects scripts into webviews
  ├── backgroundHosts: [ID: BackgroundHost]  one per enabled extension
  ├── tabIDMap: ExtensionTabIDMap        UUID ↔ chrome int (tabs)
  ├── spaceIDMap: ExtensionTabIDMap      UUID ↔ chrome int (windows)
  ├── tabObserver: ExtensionTabObserver  forwards TabStore events
  └── messageBridge: ExtensionMessageBridge  routes all extension messages

ExtensionDatabase.shared (SQLite via GRDB)
  ├── extension          metadata + manifest blob
  └── extensionStorage   key/value per extension

ExtensionToolbarManager          creates NSToolbarItems for extensions
ExtensionPopoverController       hosts popup.html in NSPopover
ExtensionInstaller               validates, copies, persists new extensions
```

## Directory Layout

```
Detour/Extensions/
├── Installer/
│   └── ExtensionInstaller.swift          validate + copy + persist
├── Model/
│   ├── WebExtension.swift                runtime model (icon, contentWorld, matchers)
│   ├── ExtensionManifest.swift           Codable MV3 manifest
│   ├── ExtensionPermissionChecker.swift  API + host permission checks
│   └── ContentScriptMatcher.swift        match-pattern → URL/JS guard logic
├── Runtime/
│   ├── ExtensionManager.swift            singleton lifecycle coordinator
│   ├── BackgroundHost.swift              hidden WKWebView for service worker
│   ├── ContentScriptInjector.swift       WKUserScript registration + ad-hoc injection
│   ├── ExtensionMessageBridge.swift      WKScriptMessageHandler message router
│   ├── ExtensionTabObserver.swift        TabStoreObserver → chrome.tabs events
│   ├── ExtensionTabIDMap.swift           UUID ↔ integer ID mapping
│   └── ChromeAPI/
│       ├── ChromeAPIBundle.swift         combines all polyfills into one bundle
│       ├── ChromeRuntimeAPI.swift        runtime.sendMessage, onMessage, getURL
│       ├── ChromeStorageAPI.swift        storage.local get/set/remove/clear
│       ├── ChromeTabsAPI.swift           tabs.query/create/update/remove/get/sendMessage
│       ├── ChromeScriptingAPI.swift      scripting.executeScript/insertCSS
│       ├── ChromeWebNavigationAPI.swift  event emitters (partially wired)
│       └── ChromeWebRequestAPI.swift     stub event emitters (no WebKit support)
├── Storage/
│   └── ExtensionDatabase.swift           GRDB database for extensions + storage API
└── UI/
    ├── ExtensionToolbarManager.swift     toolbar button factory
    └── ExtensionPopoverController.swift  popup.html in NSPopover
```

## Extension Lifecycle

### Installation

1. User picks an unpacked extension directory via file dialog.
2. `ExtensionInstaller.install(from:)` validates `manifest.json` exists and parses it as `ExtensionManifest`. Only MV3 is accepted.
3. The directory is copied to `~/Library/Application Support/Detour/Extensions/{UUID}/`.
4. An `ExtensionRecord` is saved to `ExtensionDatabase`.
5. `ExtensionManager` adds the `WebExtension` to its array, starts a `BackgroundHost`, and injects content scripts into all existing tabs.
6. A toolbar update notification triggers `BrowserWindowController` to add the extension's toolbar button.

### Enable / Disable

`ExtensionManager.setEnabled(id:enabled:)` toggles the DB flag, starts or stops the background host, and posts `extensionsDidChangeNotification`. Toolbar buttons and content script injection update accordingly.

### Uninstall

`ExtensionManager.uninstall(id:)` stops the background host, removes the extension from the in-memory array, deletes the DB record (cascading to storage), and removes the copied files from disk.

## Content Script Injection

Content scripts run in an isolated `WKContentWorld` per extension, preventing interference between extensions and page scripts.

### New webviews

`ContentScriptInjector.addContentScripts(to:)` is called during `Space.makeWebViewConfiguration()`. For each enabled extension it:

1. Generates chrome API polyfills via `ChromeAPIBundle` (with `isContentScript: true`).
2. Reads CSS files, escapes them, and wraps them in `<style>` injection code.
3. Reads JS files and wraps them with a URL match-pattern guard generated by `ContentScriptMatcher.jsGuardCondition()`.
4. Registers each script as a `WKUserScript` in the extension's content world at the appropriate injection time (`documentStart`, `documentEnd`, or `documentIdle`).

### Existing tabs

When an extension is installed or enabled after tabs are already open, `ContentScriptInjector.injectIntoExistingTab(_:for:)`:

1. Registers persistent `WKUserScript` entries on the tab's content controller (so future navigations get scripts).
2. Calls `webView.evaluateJavaScript()` to immediately inject into the current page (if the URL matches).

## Background Service Workers

`BackgroundHost` creates a hidden, off-screen `WKWebView` with a non-persistent data store. The background script is loaded by:

1. Generating chrome API polyfills via `ChromeAPIBundle` (with `isContentScript: false`).
2. Building a synthetic HTML page that inlines the polyfill bundle and the extension's service worker script in `<script>` tags.
3. Loading the HTML via `loadHTMLString`. Scripts run in `.page` world (the default WKWebView world).

The webview is marked inspectable for Safari Web Inspector debugging.

Events are dispatched to background scripts via `BackgroundHost.dispatchEvent(_:data:)`, which calls `evaluateJavaScript` with the event name and JSON payload. The `ExtensionTabObserver` uses this to forward `chrome.tabs.onCreated`, `onRemoved`, `onUpdated`, and `onActivated`.

## Message Bridge

`ExtensionMessageBridge` implements `WKScriptMessageHandler` under the handler name `"extensionMessage"`. It is registered on every content controller — in `.page` world for background/popup webviews and in the extension's content world for content scripts.

### Message flow

```
Content script                    Native bridge                    Background host
     │                                │                                 │
     ├─ runtime.sendMessage ─────────►│                                 │
     │   {type, extensionID,          │── evaluateJavaScript ──────────►│
     │    callbackID, data}           │   __extensionDispatchMessage()  │
     │                                │                                 │
     │                                │◄── runtime.sendResponse ────────┤
     │◄── __extensionDeliverResponse──│    {callbackID, response}       │
     │                                │                                 │
```

### Supported message types

| Type | Direction | Description |
|------|-----------|-------------|
| `runtime.sendMessage` | content → background | Content script sends message to background |
| `runtime.sendResponse` | background → content | Response routed back via callback ID |
| `storage.get` | any → native | `chrome.storage.local.get` |
| `storage.set` | any → native | `chrome.storage.local.set` |
| `storage.remove` | any → native | `chrome.storage.local.remove` |
| `storage.clear` | any → native | `chrome.storage.local.clear` |
| `tabs.query` | background/popup → native | Query open tabs with filters |
| `tabs.create` | background/popup → native | Open a new tab |
| `tabs.update` | background/popup → native | Navigate or activate a tab |
| `tabs.remove` | background/popup → native | Close tab(s) |
| `tabs.get` | background/popup → native | Get single tab info |
| `tabs.sendMessage` | background → content | Send message to a tab's content script |
| `scripting.executeScript` | background/popup → native | Inject JS into a tab |
| `scripting.insertCSS` | background/popup → native | Inject CSS into a tab |

Responses are routed back to the correct webview and content world using the `PendingResponse` struct, which stores the source webview and target world.

### Permission enforcement

Before dispatching any message, the bridge checks permissions via `ExtensionPermissionChecker`:

1. **API permission gate** — `requiredPermission(for:)` maps the message type prefix (`storage`, `tabs`, `scripting`) to a required manifest permission. If the extension lacks it, the bridge returns an error to the callback without processing the request. `runtime.*` messages are always allowed.
2. **Host permission gate** — `tabs.sendMessage`, `scripting.executeScript`, and `scripting.insertCSS` additionally check `hasHostPermission(for:extension:)` against the target tab's URL. If the URL doesn't match any `host_permissions` pattern, the call is rejected.

## Chrome API Polyfills

`ChromeAPIBundle.generateBundle(for:isContentScript:)` concatenates all polyfill generators into a single JavaScript string. The `isContentScript` flag controls world isolation: content scripts get polyfills injected into the extension's `WKContentWorld`, while background and popup scripts get them in `.page` world.

All APIs use `webkit.messageHandlers.extensionMessage.postMessage()` to communicate with native code.

| API | Support | Notes |
|-----|---------|-------|
| `chrome.runtime.id` | Full | Extension ID string |
| `chrome.runtime.getManifest()` | Full | Returns parsed manifest |
| `chrome.runtime.getURL(path)` | Full | Returns `extension://{id}/path` |
| `chrome.runtime.sendMessage()` | Full | Promise + callback, routed via bridge |
| `chrome.runtime.onMessage` | Full | Event emitter with addListener/removeListener |
| `chrome.storage.local.get/set/remove/clear` | Full | Backed by SQLite, promise + callback |
| `chrome.tabs.query()` | Full | Supports active, currentWindow, url, title filters |
| `chrome.tabs.create/update/remove/get` | Full | Full CRUD on tabs |
| `chrome.tabs.sendMessage()` | Full | Background → content script messaging |
| `chrome.tabs.onCreated/Removed/Updated/Activated` | Full | Fired via ExtensionTabObserver |
| `chrome.scripting.executeScript()` | Full | func + args or files |
| `chrome.scripting.insertCSS()` | Full | Inline css or files |
| `chrome.scripting.removeCSS()` | Stub | No-op (WebKit limitation) |
| `chrome.webNavigation.*` | Full | All four events fire from WKNavigationDelegate |
| `chrome.webRequest.*` | Stub | No-op emitters; WebKit has no request interception |

## Popup / Toolbar UI

`ExtensionToolbarManager` generates `NSToolbarItem` identifiers for enabled extensions that declare an `action` in their manifest. Each toolbar item gets an `NSButton` with the extension's icon (scaled to 28x22) or a fallback puzzle-piece icon.

When the toolbar button is clicked, `BrowserWindowController` (conforming to `ExtensionToolbarActions`) creates an `ExtensionPopoverController` which:

1. Creates a `WKWebView` with chrome API polyfills injected.
2. Loads the extension's `popup.html` from its base path.
3. Presents in an `NSPopover` (360x480, transient behavior).

The popover's webview gets the same message bridge registration as background hosts, so popup scripts can use `chrome.tabs`, `chrome.storage`, etc.

## Storage

`ExtensionDatabase` is a separate SQLite database at `~/Library/Application Support/Detour/extensions.db`, managed via GRDB.

### Schema

**`extension`** table:

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID string |
| `name` | TEXT | Extension name |
| `version` | TEXT | Version string |
| `manifestJSON` | BLOB | Encoded manifest |
| `basePath` | TEXT | File system path to extension files |
| `isEnabled` | BOOLEAN | Enabled flag |
| `installedAt` | DOUBLE | Unix timestamp |

**`extensionStorage`** table:

| Column | Type | Description |
|--------|------|-------------|
| `extensionID` | TEXT | FK to `extension.id` |
| `key` | TEXT | Storage key |
| `value` | BLOB | JSON-encoded value |

Primary key: (`extensionID`, `key`). Foreign key cascades deletes — removing an extension clears its storage.

## Tab ID Mapping

Chrome APIs use integer IDs for tabs and windows. Detour uses UUIDs internally. `ExtensionTabIDMap` provides a session-scoped bidirectional mapping:

- `intID(for: UUID) -> Int` — returns existing mapping or auto-assigns the next integer (starting at 1).
- `uuid(for: Int) -> UUID?` — reverse lookup.
- Mappings are cleaned up when tabs/spaces are removed via `ExtensionTabObserver`.

Two instances exist: `ExtensionManager.tabIDMap` for tab IDs and `ExtensionManager.spaceIDMap` for window IDs.

## Test Extensions

Two test extensions live under `TestExtensions/`:

- **hello-world** — Minimal extension with a content script (runs on all URLs), background service worker, popup, and storage permission. Useful for verifying basic lifecycle and injection.
- **api-explorer** — Comprehensive extension exercising tabs, scripting, storage, webNavigation, and webRequest APIs. Declares host permissions for all URLs. Includes an `inject.js` and `inject.css` for testing `chrome.scripting` APIs.

## Limitations

- **`chrome.webRequest`** — Stub-only event emitters. WebKit provides no pre-request interception API; a warning is logged on first listener registration.
- **`chrome.webNavigation`** — All four events (`onBeforeNavigate`, `onCommitted`, `onCompleted`, `onErrorOccurred`) fire from `WKNavigationDelegate` methods, but only for main-frame navigations (`frameId: 0`). Sub-frame navigation events are not tracked.
- **`chrome.scripting.removeCSS`** — No-op. WebKit does not support targeted CSS removal.
- **No runtime permission grant/revoke UI** — Permissions are shown at install time via an `NSAlert` confirmation prompt (using `ExtensionPermissionChecker.permissionSummary`), and the bridge enforces them at the API level, but there is no UI for users to selectively grant or revoke individual permissions after installation.
- **No extension settings UI** — Extensions can only be installed/uninstalled via the debug menu. There is no dedicated settings panel for managing extensions.
- **Unpacked only** — No `.crx` or Chrome Web Store support. Extensions must be loaded from local directories.
